program Estoque;

uses
  Forms,
  U_Principal in 'U_Principal.pas' {FEntrada},
  MenuInicial in 'MenuInicial.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TFEntrada, FEntrada);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
